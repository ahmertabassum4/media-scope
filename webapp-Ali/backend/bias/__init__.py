# Vendored bias-prediction pipeline (from the Oleg branch's application/models/app).
# Self-contained stacked ensemble: gpt-5.5 vision label + PaddleOCR text + SigLIP
# embedding -> meta-model. Independent of Ali's factuality feature pipeline.

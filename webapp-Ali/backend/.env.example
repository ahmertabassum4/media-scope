# Copy to .env and paste your OpenRouter key after the = sign.
# The same key powers factuality (joint verdict + explain) and the bias path.
OPENROUTER_API_KEY=

# Optional: override the vision model used by the bias ensemble (defaults to openai/gpt-5.5).
# BIAS_OPENROUTER_MODEL=openai/gpt-5.5
